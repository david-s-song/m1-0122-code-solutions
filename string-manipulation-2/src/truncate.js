/* exported truncate */
function truncate(length, string) {
  return string.substring(0, length) + '...';
}

/*
1. Define a function with 2 parameters
2. Use the substring method to grab characters from the start to the given length and concatenate with string ellipsis
3. Return the value of expression
*/
