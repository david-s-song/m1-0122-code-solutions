/* exported last */
function last(array) {
  return array[array.length - 1];
}

/*
1. Define the function with one parameter
2. Define the last item in the parameter object
3. Return the last item of the parameter object
*/
