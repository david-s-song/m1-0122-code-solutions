/* exported takeRight */
function takeRight(array, count) {
  return array.slice(-count, array.length);
}

/*
1. Define a function with 2 parameters
2. Use the slice method to count backwards from the start and end with the length of the array
3. Return the value of array
*/
