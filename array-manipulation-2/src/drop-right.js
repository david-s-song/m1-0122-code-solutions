/* exported dropRight */
function dropRight(array, count) {
  return array.slice(0, -count);
}

/*
1. Define a function with 2 parameters
2. Use the slice method to start pushing from the first item to the count backwards
3. Return the value of array
*/
