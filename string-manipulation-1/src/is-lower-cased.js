/* exported isLowerCased */
function isLowerCased(word) {
  return word === word.toLowerCase();
}

/*
1. Define a function with one parameter
2. Use toLowerCase method to make the parameter object lowercased
3. Use comparative expression between the parameter object and the lowercased parameter object
4. Return the expression which will either be true or false
*/
