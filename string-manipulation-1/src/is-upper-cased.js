/* exported isUpperCased */
function isUpperCased(word) {
  return word === word.toUpperCase();
}

/*
1. Define a function with one parameter
2. Use toUpperCase method to make the parameter object uppercased
3. Use comparative expression between the parameter object and the uppercased parameter object
4. Return the expression which will either be true or false
*/
