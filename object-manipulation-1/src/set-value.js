/* exported setValue */
function setValue(object, key, value) {
  object[key] = value;
}

/*
1. Define a function with 3 parameters
2. Attach key parameter to the object parameter and assign it to the value parameter
*/
